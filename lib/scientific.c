#include "scientific.h"

/* Your code here */

float sinOfDegree(float num)
{
	float x = num * (PI / 180);
	return sin(x);
}

float cosOfDegree(float num)
{
	float x = num * (PI / 180);
	return cos(x);
}
